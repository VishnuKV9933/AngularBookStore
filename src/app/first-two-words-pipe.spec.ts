import { FirstTwoWordsPipe } from './first-two-words-pipe';

describe('FirstTwoWordsPipe', () => {
  it('should create an instance', () => {
    expect(new FirstTwoWordsPipe()).toBeTruthy();
  });
});
